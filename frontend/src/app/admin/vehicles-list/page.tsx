import React from 'react';
import VehicleList from '../../../modules/admin/vehicles-list/vehicles'; // Adjust the path based on your folder structure

const VehicleListPage: React.FC = () => {
  return (
    <div>
      <VehicleList />
    </div>
  );
};

export default VehicleListPage;